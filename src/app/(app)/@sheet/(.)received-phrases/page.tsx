import { Sheet, SheetHeader, SheetTitle } from '@/components/ui/sheet';

export default function CreateOrganization() {
  return (
    <Sheet defaultOpen>
      <SheetHeader>
        <SheetTitle>intercepted</SheetTitle>
      </SheetHeader>
    </Sheet>
  );
}
